import { trpc } from '../utils/trpc';
import React from 'react';

export const Hello = () => {
	return (
		<div>
			<button>ayo lets do that login thang</button>
			{/* <button onClick={testProtected}>protected</button> */}
		</div>
	);
};
